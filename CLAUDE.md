# CLAUDE.md

Claude Code should use `AGENTS.md` as the canonical agent guidance for this
repository.

This file is intentionally a compatibility pointer so repository instructions do
not drift between agent surfaces. When behavior, commands, architecture, or
verification expectations change, update `AGENTS.md`.
